spring:
  application:
    #服务名字
    name: eureka
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8762/eureka/
      #是否注册到注册中心(服务端本身不注册)
    register-with-eureka: false
  server:
    #关闭自我保护机制
    enable-self-preservation: false
server:
  port: 8761